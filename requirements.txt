flask>=3.0.0
flask-socketio>=5.3.0
eventlet>=0.35.0
gunicorn>=22.0.0
anthropic>=0.40.0
openai>=1.30.0
google-generativeai>=0.7.0
groq>=0.9.0
PyGithub>=2.3.0
reportlab>=4.2.0
defusedxml>=0.7.1 # secure XML parsing (XXE-safe) for Maven pom.xml dependency parsing
docker>=7.0.0
semgrep>=1.70.0
pip-audit>=2.7.0
urllib3>=1.26.19 # not directly required, pinned by Snyk to avoid a vulnerability
pyjwt>=2.11.0 # not directly required, pinned by Snyk to avoid a vulnerability
requests>=2.32.2 # not directly required, pinned by Snyk to avoid a vulnerability
idna>=3.15 # not directly required, pinned by Snyk to avoid a vulnerability

# Optional — install for additional scan capabilities
# detect-secrets>=1.4.0    # secret scanning (--secret-scan)
# checkov>=3.0.0           # IaC scanning (--iac-scan)
# docker>=7.0.0            # sandbox execution
# yara-python>=4.3.0       # YARA rule scanning
# weasyprint>=60.0         # PDF report export (--pdf)
# rq>=1.16.0               # Redis job queue (optional upgrade from threading queue)
# redis>=5.0.0             # Redis backend for rq
