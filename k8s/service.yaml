apiVersion: v1
kind: Service
metadata:
  name: secscope
  labels:
    app: secscope
spec:
  type: ClusterIP
  selector:
    app: secscope
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
